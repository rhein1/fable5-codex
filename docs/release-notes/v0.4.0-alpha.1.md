# Fable-5 for Codex v0.4.0-alpha.1

This follow-up keeps the Sol Ultra package and benchmark results unchanged while fixing the public benchmark citations.

## Fixed

- Raw benchmark reports now link to the canonical `evals/` and `examples/` fixtures on GitHub.
- Future benchmark runs normalize isolated workspace paths before scoring or publishing output.
- Package validation rejects absolute Windows destinations in the latest benchmark reports.

## Install

```powershell
npx github:rhein1/fable5-codex#v0.4.0-alpha.1
```
