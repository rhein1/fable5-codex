# Fable-5 for Codex v0.4.0-alpha.2

This follow-up makes the new Sol Ultra benchmark charts unambiguously visible on GitHub.

## Fixed

- The root and benchmark READMEs now reference run-specific chart filenames for `20260713T234332Z`.
- The benchmark renderer generates immutable run-specific chart files alongside the stable compatibility files.
- Package validation prevents future releases from silently reusing generic benchmark image URLs.

The underlying benchmark data is unchanged: average composite `81.7 -> 100.0`, with average wall time `144.5s -> 344.0s`.

## Install

```powershell
npx github:rhein1/fable5-codex#v0.4.0-alpha.2
```
