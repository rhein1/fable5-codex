# Copy these settings into ~/.codex/config.toml or a trusted repo's .codex/config.toml.
model = "gpt-5.6-sol"
model_reasoning_effort = "ultra"

[agents]
# Ultra coordinates four agents by default. Leave headroom for the coordinator
# and verifier while preventing recursive fan-out.
max_threads = 6
max_depth = 1
